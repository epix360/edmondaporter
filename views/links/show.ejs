<% layout('/layouts/boilerplate') %>
    <div class="row">
        <div class="col">
            <h1>Links</h1>
            <div>
                <%-link.content%>
            </div>
        </div>
        <% if(currentUser && profile.equals(currentUser._id)) { %>
            <div class="col">
                <div class="text-end">
                    <div class="mb-3">
                        <a class="btn btn-info" href="/links/<%=link._id%>/edit">Edit Publication List</a>
                    </div>
                </div>
            </div>
            <% } %>
    </div>